<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SudOcean" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.SudOcean" parent="Base.Theme.SudOcean" />

    <!-- Стили для экрана деталей (необходимы для CollapsingToolbarLayout) -->
    <style name="Theme.SudOcean.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.SudOcean.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
