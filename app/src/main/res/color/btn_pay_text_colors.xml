<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Цвет текста, когда кнопка выключена (пустая корзина) -->
    <item android:state_enabled="false" android:color="@color/ocean_text_secondary" />

    <!-- Цвет текста по умолчанию (кнопка активна) -->
    <item android:color="@color/white" />
</selector>
